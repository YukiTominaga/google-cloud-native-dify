apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dify.fullname" . }}-ssrf-proxy-config
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/component: ssrf-proxy
data:
  squid.conf.template: |
{{ .Files.Get "files/ssrf-proxy/squid.conf.template" | indent 4 }}
  docker-entrypoint.sh: |
{{ .Files.Get "files/ssrf-proxy/docker-entrypoint.sh" | indent 4 }}
